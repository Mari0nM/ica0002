[server]
root_url = http://{{ hostvars[groups['web_servers'][0]].ansible_host }}:{{ hostvars[groups['web_servers'][0]].ansible_port + 58 }}/grafana/
serve_from_sub_path = true

[security]
admin_user = {{ grafana_user }}
admin_password = {{ grafana_password }}
