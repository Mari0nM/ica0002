[server]
root_url = http://{{ inventory_hostname }}:{{ hostvars[inventory_hostname].ansible_port + 58 }}/grafana/
serve_from_sub_path = true

[security]
admin_user = {{ grafana_user }}
admin_password = {{ grafana_password }}
